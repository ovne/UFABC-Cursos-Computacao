//#include "determinante.h"
#include <stdio.h>

/*- Esta função deve preenher a matriz m_saida com todos os elementos de m_entrada, mas removendo
* a primeira linha e a coluna c:
*- A matriz m_entrada não deve ser alterada; Apenas a matriz m_saida é preenchida.
*- Não é preciso alocar nenhuma matriz nesta função, as matrizes m_entrada e m_saida devem ser pré-alocadas
*por quem chamar esta função.
*- A ordem da matriz de entrada é representada pelo parâmetro n da função.
*- Considere que o parâmetro c inicia no valor 0 (0 é a primeira coluna).
*- Não é necessário retornar a ordem da matriz de retorno, pois sempre será n-1. */
void remove_primeira_linha_e_coluna_c(int n, float m_entrada[n][n], float m_saida[n-1][n-1], int c) {
    // ja tenho os valores de m_entrada 
}

float calcula_determinante(int n, float m_entrada[n][n]) {
    // Implementar (esta funcao deve ser recursiva)
}

int main() {
    // Implementar
    
    return 0;
}